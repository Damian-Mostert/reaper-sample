{
    "name": "reaper-example",
    "version": "0.1.0-example",
    "scripts": {
        "dev": "npx reaperjs dev",
        "build": "npx reaperjs build",
        "start": "npx reaperjs start",
        "make": "npx reaperjs make",
        "migrate": "npx reaperjs migrate",
        "rollback": "npx reaperjs rollback",
        "seed": "npx reaperjs seed"
    },
    "dependencies": {
        "ejs": "latest",
        "reaperjs": "latest"
    },
    "devDependencies": {
        "@types/react": "^18"
    }
}
